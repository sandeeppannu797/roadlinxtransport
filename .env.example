# SMTP credentials for the quote and contact forms.
# Both forms send to info@roadlinxtransport.com.au from
# admin@roadlinxtransport.com.au, with the submitter as Reply-To.
# Use the mailbox on the hosting account that admin@ belongs to.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
